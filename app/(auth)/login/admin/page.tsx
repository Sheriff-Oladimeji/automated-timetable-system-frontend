import { LoginForm } from '@/components/auth/login-form'

export default function AdminLoginPage() {
  return (
    <LoginForm
      title="Administrator Sign In"
      description="Sign in to manage the timetable system"
    />
  )
}
